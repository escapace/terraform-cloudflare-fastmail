{
  "name": "@terraform/cloudflare-fastmail",
  "version": "0.0.0",
  "devDependencies": {
    "@commitlint/cli": "17.7.1",
    "@ls-lint/ls-lint": "v2.1.0-beta.0",
    "husky": "8.0.3",
    "is-ci": "3.0.1",
    "lint-staged": "14.0.1",
    "prettier": "3.0.2",
    "prettier-config-escapace": "1.0.6"
  },
  "private": true,
  "scripts": {
    "docs": "terraform-docs . && prettier --write README.md",
    "lint": "ls-lint && terraform fmt && tflint --chdir . && tfsec .",
    "lint-staged": "lint-staged",
    "prepare": "terraform init -backend=false && tflint --init && (is-ci || husky install)",
    "typecheck": "terraform validate"
  },
  "type": "module"
}
